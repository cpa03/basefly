# Issue Manager & Audit — 2026-08-19 (loop199)

> Generated by the autonomous maintenance loop (`/ulw-loop`). This report documents: Phase 0 entry decision, ISSUE MANAGER MODE analysis, capability re-probes, verified stale-resolution matrix for all P0/P1 issues, and the still-blocked CI/CD repair target.

---

## 1. Execution Summary

| Field          | Value                                                                   |
| -------------- | ----------------------------------------------------------------------- |
| Date           | 2026-08-19                                                              |
| Default branch | `main` (auto-detected)                                                  |
| Open PRs       | 0 (at entry)                                                            |
| Open issues    | 82                                                                      |
| Active phase   | ISSUE MANAGER MODE (blocked) → FAIL-SAFE documentation                  |
| Final state    | **blocked** (see §5)                                                    |

### Phase 0 — Entry Decision

- STEP 0.1: `gh pr list --state open` → **0 open PRs** → proceed to issues.
- STEP 0.2: `gh issue list --state open` → **82 open issues** → ISSUE MANAGER MODE entered.
- No new PRs or issues created since loop198 (issue count stable at 82; HEAD `2fa22bc` == `origin/main`).

---

## 2. Capability Probes (re-verified this loop, fresh evidence)

| Operation                    | Result                                                                                        |
| ---------------------------- | --------------------------------------------------------------------------------------------- |
| Read issues / PRs / repo     | ✅                                                                                             |
| `gh issue comment`           | ❌ 403 `Resource not accessible by integration (addComment)`                                   |
| `gh issue edit --add-label`  | ❌ 403 `(addLabelsToLabelable)`                                                               |
| `gh issue edit --remove-label` | ❌ 403 `(removeLabelsFromLabelable)`                                                         |
| `gh issue create`            | ❌ 403 `(createIssue)`                                                                         |
| `git push` (non-workflow)    | ✅ verified with real push + delete of `probe/test-push`                                       |
| `git push` (workflow file)   | ❌ rejected: _"refusing to allow a GitHub App to create or update workflow ... without `workflows` permission"_ (verified with real `.github/workflows/probe-test.yml` push attempt) |
| PR merge (`--admin`)         | ✅ (precedent: #1402, #1403 merged by prior loops)                                             |

**Root cause unchanged:** `.github/workflows/on-pull.yml` `permissions:` block grants `contents/pull-requests/actions/repository-projects/id-token` but **lacks `issues: write`**; the GitHub App token additionally lacks `workflows`. Issue management and workflow-file repair remain blocked at the token level. Fixing the workflow permissions block itself requires a workflow-file push → circular block.

---

## 3. ISSUE MANAGER MODE — Analysis (blocked from execution)

- **STEP 1 Normalization** (38 issues missing priority label, ~8 missing category): ❌ blocked (no `issues: write`).
- **STEP 2 Dedup** (5 clusters: rate-limiter #480/#496, pnpm-CI #305/#584/#595/#670/#744, E2E #501/#628/#724, router-tests #551/#631/#725, auth-tests #500/#549): ❌ blocked.
- **STEP 3 Consolidation** (testing cluster #500/#549/#550/#551/#581): ❌ blocked (consolidation issue #581 already exists and documents the group).
- **STEP 4 Repair Mode**: selection performed, verdict below.

### Repair Mode selection

**Selection rule:** P0/P1 exists → highest-priority issue. **All P0/P1 issues verified stale-resolved in code this loop** (see matrix). No P0/P1 code work remains. The highest-value genuine gap is the CI/CD cluster — **blocked** (workflow push denied).

### Verified stale-resolution matrix (fresh evidence, this loop)

| Issue | Priority | Verdict | Evidence (re-verified loop199) |
| ----- | -------- | ------- | ------------------------------ |
| #496  | P0       | Resolved | `packages/api/src/distributed-rate-limiter.ts` (Redis sliding window + in-memory fallback); `trpc.ts` imports `getLimiter`/`checkAsync` from it; tests `distributed-rate-limiter.test.ts` + `-sync.test.ts` exist |
| #480  | P1       | Resolved (dup of #496) | Same distributed rate limiter code path |
| #498  | P1       | Resolved | `requireRole` + `isAdmin` middleware in `trpc.ts` (lines 254+); DB-role based; `rbac.test.ts` + `authorization.test.ts` exist |
| #515  | P1       | Resolved | `csrfProtection` middleware wired into base `procedure` (`trpc.ts:104`, `:215`); `apps/nextjs/src/lib/csrf.ts` + `csrf.test.ts` exist |
| #500  | P1       | Resolved | `packages/auth/clerk.test.ts`, `env.test.ts`, `logger.test.ts` exist |
| #549  | P1       | Resolved | Same auth test files |
| #501  | P1       | Resolved | `tests/e2e/` — 11 spec files (admin, auth, authorization-bypass, billing, cluster, critical-flows, dashboard, home, pricing, subscription-workflows, webhook-error-handling) |
| #551  | P1       | Resolved | `packages/api/src/router/k8s.test.ts` + `k8s-router.test.ts` exist |
| #550  | P1       | Resolved | `vitest.config.ts:16` — `include: ["packages/**/*.{ts,tsx}", "apps/nextjs/src/**/*.{ts,tsx}"]` |
| #581  | P1       | Resolved | All consolidated sub-issues (#549/#550/#551/#500/#501) resolved individually |

**Spot-checked P2/P3 resolved claims** (code evidence this loop): #785 (stripe package.json has no duplicate `next` — single clean deps block), #786 (webhook route logs only `identifier/requestId/resetAt` — no secret slice), #720/#748 (`.nvmrc` = `22.14.0`, valid), #789 (`packages/ui/package.json` has `peerDependencies: react/react-dom ^19`), #755 (migration `20260227_add_customer_subscription_composite_index` present), #754 (`packages/stripe/src/webhook-idempotency.ts` + `.test.ts` present), #722 (`apps/nextjs/src/instrumentation.ts` startup env validation).

---

## 4. Genuinely Unresolved — CI/CD Cluster (still blocked)

| Item | Evidence (this loop) |
| ---- | -------------------- |
| `iterate.yml:72`, `:342` | `npm ci \|\| true` in a pnpm repo |
| `iterate.yml:58-59` | Cache path `~/.npm` + key `hashFiles('**/package-lock.json')` (should be pnpm store + `pnpm-lock.yaml`) |
| Node version pins | `iterate.yml:70/266/340/395` and `on-pull.yml:55` pin `node-version: "20"` while `.nvmrc`=`22.14.0` and `package.json` `engines.node`=`>=22` → CI build breaks on Node 20 |
| `on-pull.yml` permissions | Lacks `issues: write` (blocking issue management) |

**Fix status:** fully prepared in prior loops (loop197 §6, commit `72887ba`, branch `fix/ci-node22-and-pnpm-consistency`) but **cannot be pushed**: workflow-file pushes are rejected by the GitHub App token (no `workflows` permission). Re-verified with a real push attempt this loop. Requires maintainer action.

---

## 5. Final State

**State: BLOCKED** (with reasons)

| Blocked action                                        | Reason                                                              |
| ----------------------------------------------------- | ------------------------------------------------------------------- |
| Issue normalization / dedup / consolidation / closure | Token lacks `issues: write` (403 verified on comment/label/create)  |
| Push CI workflow fix (Node 22 + pnpm consistency)     | Token lacks `workflows` permission (push rejection re-verified)     |
| Create issues from findings                           | Token lacks `issues: write`                                         |
| Repair P0/P1 issues                                   | **None remain** — all verified resolved in code (see §3 matrix)     |
| Local build/test verification                         | Local env Node v20.20.2 (project requires >=22); no `node_modules` installed |

**Recommended next actions for a maintainer (or a run with full permissions):**

1. Push branch `fix/ci-node22-and-pnpm-consistency` (commit `72887ba`) — unblocks CI builds (Node 22) and issue management permissions.
2. Execute the normalization table from loop197 §2 (45 label operations).
3. Close stale-resolved issues per §3 matrix (10 P0/P1 + spot-checked P2/P3 with code evidence).
4. Track the `@opentelemetry/core` moderate advisory (GHSA-8988-4f7v-96qf); revisit when contentlayer2 supports OTel 2.x.

---

## 6. Actions Taken This Loop

| Timestamp (UTC) | Action                                   | Target                                    | Result                       |
| --------------- | ---------------------------------------- | ----------------------------------------- | ---------------------------- |
| ~04:00          | Phase 0 entry scan                       | PRs + issues                              | 0 PRs, 82 issues → ISSUE MANAGER MODE |
| ~04:01          | Capability probes                        | issues (comment/label/create), push       | ❌ 403 / ❌ workflow-rejected / ✅ non-workflow push |
| ~04:02          | Stale-resolution re-verification         | all P0/P1 issues + spot-checked P2/P3     | ✅ all stale-resolved (fresh evidence) |
| ~04:05          | CI/CD cluster re-verification            | iterate.yml / on-pull.yml                 | ❌ still unresolved, push blocked |
| ~04:10          | This report                              | `docs/issue-manager-audit-2026-08-19-loop199.md` | ✅ created |

---

## 7. Decision Summary

- **Why this phase ran:** Phase 0 found 0 open PRs and 82 open issues → ISSUE MANAGER MODE per the state machine.
- **Why blocked:** Token-level permissions (`issues: write`, `workflows`) are absent; all P0/P1 repair targets are verified resolved in code. Per FAIL-SAFE rule, no destructive or unverifiable action was taken.
- **Next viable unblocked action:** none within token scope beyond documenting state (this report).
