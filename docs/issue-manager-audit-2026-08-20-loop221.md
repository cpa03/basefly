# Issue Manager & Audit — 2026-08-20 (loop221)

> Generated by the autonomous maintenance loop (`/ulw-loop`). This report documents: Phase 0 entry decision, ISSUE MANAGER MODE (STEP 1–3 token-blocked status, STEP 4 repair selection), fresh capability probes, fresh P0/P1/P2 regression spot-checks, a **new fresh probe: existing-workflow-file modification is also 403-blocked** (previously only new-file creation was probed), full local verification suite results, and final state.

---

## 1. Execution Summary

| Field          | Value                                                                                    |
| -------------- | ---------------------------------------------------------------------------------------- |
| Date           | 2026-08-20                                                                               |
| Default branch | `main` (auto-detected)                                                                   |
| Open PRs       | 0 at entry                                                                               |
| Open issues    | 82 (unchanged from loop218–220)                                                          |
| Active phase   | ISSUE MANAGER MODE (Phase 0)                                                             |
| Final state    | **idle** (no actionable repair target within token permissions; all findings documented) |

### Phase 0 — Entry Decision

- STEP 0.1: `gh pr list --state open --limit 5` → **0 open PRs**.
- STEP 0.2: `gh issue list --state open --limit 100` → **82 open issues** → ISSUE MANAGER MODE entered.
- (STEP 0.3 / Phase 1 not reached.)

---

## 2. ISSUE MANAGER MODE — STEP 1–3 (Normalization / Dedup / Consolidation)

**Status: token-blocked** (fresh probes this loop, §3). Cannot assign category/priority labels, cannot comment, cannot close, cannot create issues. No label normalization, duplicate closure, or small-issue consolidation was possible.

**Duplicate clusters identified for a maintainer (unchanged from prior loops, re-confirmed against fresh issue list):**

- Rate limiter: #496 (P0) / #480 (P1) — canonical #496
- pnpm consistency: #305 / #584 / #595 / #670 / #744 — canonical #305
- E2E testing: #501 / #628 / #724 — canonical #501
- Testing sub-issues: consolidated under #581 (#549/#550/#551/#631/#725/#787/#788/#713/#754)
- .nvmrc: #720 / #748 — canonical #748 (both resolved in code)
- AI/innovation proposals: #727 / #749 / #668 / #731 — product decisions, not duplicates

---

## 3. Capability Probes (this loop, fresh evidence)

| Operation                      | loop220 | loop221 (fresh probe)                                                                                            |
| ------------------------------ | ------- | ---------------------------------------------------------------------------------------------------------------- |
| `gh issue edit --add-label`    | ❌ 403  | ❌ 403 `addLabelsToLabelable` (fresh probe on #789)                                                               |
| `gh issue comment`             | ❌ 403  | ❌ 403 `addComment` (fresh probe on #789)                                                                         |
| `gh issue close`               | ❌ 403  | ❌ 403 `closeIssue` (fresh probe on #789)                                                                         |
| `gh issue create`              | ❌ 403  | ❌ 403 `createIssue` (fresh probe)                                                                                |
| Push: **new** workflow file    | ❌ 403  | ❌ 403 `workflows` guard (fresh probe, `.github/workflows/.probe`)                                                 |
| Push: **existing** workflow file| n/a     | ❌ 403 **NEW PROBE**: applied `docs/ci/iterate-pnpm-fix.patch` to `iterate.yml`, commit + push → rejected `refusing to allow a GitHub App to create or update workflow .github/workflows/iterate.yml without workflows permission`; local branch cleaned up |
| `git push` (non-workflow)      | ✅      | ✅ (PR push path verified in prior loops; not re-attempted this loop)                                             |
| `gh pr create` / `gh pr merge` | ✅      | ✅ (loop217 merged #1429; pattern re-confirmed)                                                                   |

**Key new finding this loop:** the `workflows` permission guard blocks **modification of existing workflow files**, not just creation of new ones. This definitively closes the #305/#744/#670 repair path (documented patch `docs/ci/iterate-pnpm-fix.patch` applies cleanly and is correct, but cannot be pushed by this token).

---

## 4. STEP 4 — Repair Mode

**Selection:** P0/P1 issues exist in the tracker (#496 P0; #480/#498/#500/#501/#515/#549/#550/#551/#581 P1) but **all are verified resolved in code** (fresh spot-checks this loop, §5). Per the state-machine fallback → lowest-scoring domain/criterion → CI/CD Health → #305 (pnpm consistency in `iterate.yml`), which requires `workflows` permission → **token-blocked** (fresh probe this loop on an existing-file modification; see §3).

**Outcome:** no actionable repair target exists within token permissions and the minimal-atomic constraint. Final state: idle.

---

## 5. Fresh Verification Evidence (this loop, independent of loop220)

### P0/P1 spot-checks (fresh greps/reads on `main` @ `e6a3607`)

| #        | Verdict | Evidence (this loop, fresh)                                                                                                                                                                                                                       |
| -------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 496 (P0) | R       | `packages/api/src/distributed-rate-limiter.ts` exists (Redis sliding-window + `InMemoryRateLimiter` fallback); `trpc.ts` imports `getLimiter` (line 17) and calls `limiter.checkAsync` (line 439); `docs/redis-setup.md` exists for setup/configuration |
| 480 (P1) | R       | duplicate of #496 (same Redis rate-limiter topic)                                                                                                                                                                                                 |
| 498 (P1) | R       | `packages/api/src/rbac.ts` + `authorization.ts` with tests (`rbac.test.ts`, `authorization.test.ts`); admin guards DB-backed (commit a4b821c)                                                                                                     |
| 500 (P1) | R       | `apps/nextjs/src/utils/clerk.test.ts` exists; middleware tests added (commit ea9c18f)                                                                                                                                                             |
| 501 (P1) | R       | `tests/e2e/*.spec.ts` present: cluster, home, billing, webhook-error-handling, authorization-bypass                                                                                                                                               |
| 515 (P1) | R       | `apps/nextjs/src/lib/csrf.ts` + Origin/Referer validation in `proxy.ts` (lines 16–93) and tRPC edge route (commit 1a3927d)                                                                                                                         |
| 549 (P1) | R       | `packages/auth/clerk.test.ts`, `env.test.ts`, `logger.test.ts` exist                                                                                                                                                                              |
| 550 (P1) | R       | `vitest.config.ts` includes `apps/nextjs/src/**/*.{ts,tsx}` (line 16) with setup file (line 12)                                                                                                                                                    |
| 551 (P1) | R       | `packages/api/src/router/k8s-router.test.ts` + `k8s.test.ts` exist                                                                                                                                                                                |
| 581 (P1) | R       | consolidated testing infra: `vitest.config.ts` + 148 test files / 2166 tests green this loop                                                                                                                                                       |

### Newest issues (#785–#789, created 2026-02-27) — fresh verdicts

| #        | Verdict | Evidence (this loop, fresh)                                                                                                                                                          |
| -------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 785 (bug)| R       | `packages/stripe/package.json` has **no** `next` dependency (deps: common, db, t3-env, stripe, zod) — duplicate removed |
| 786 (sec)| R       | `apps/nextjs/src/app/api/webhooks/stripe/route.ts` logs only `{identifier: "stripe-webhook", requestId, resetAt}` on rate-limit; no secret slicing anywhere; security comments on StripeError handling (lines 150–158); `sensitive-data-logging.test.ts` exists |
| 787 (test)| R      | `packages/db/migrations.test.ts` + seed/soft-delete/rls tests exist                                                                                                                    |
| 788 (test)| R      | `apps/nextjs/src/components/__tests__/` has 15+ component tests (cluster-*, navbar, modal, skip-link, etc.)                                                                             |
| 789 (enh)| R       | `packages/ui/package.json` has `peerDependencies: { next: ">=14.0.0", react: "^19.0.0", react-dom: "^19.0.0" }`                                                                         |

### Previously-"partial" P2 items — re-verified fresh

| #        | Verdict | Evidence (this loop, fresh)                                                                                                                                                                                                                           |
| -------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 688 (P2) | R       | `apps/nextjs/src/proxy.ts` implements the middleware role: security headers (lines 107–126), Clerk auth redirects (lines 256–266), CSRF gate (line 242), request ID + logging. `middleware.ts` intentionally removed in favor of `proxy.ts` (commits db1070a, 385c551) |
| 663 (P2) | R       | only 6 `eslint-disable` comments remain in non-test code, all with explicit justification comments (e.g. `-- intentional truthy check`) — consolidation complete                        |
| 664 (P2) | R       | remaining `console.*` in `packages/db` + `packages/stripe` are all inside JSDoc comment examples, not executable code                                                                    |
| 697 (docs)| R      | no mojibake/corrupted-text matches in `docs/*.md` (scan for `�`/`â€`/`Ã©` clean)                                                                                                        |
| 636 (P2) | R       | ISR/revalidate present in dashboard page, health route, tRPC edge route                                                                                                                                                                                |
| 684 (P3) | R       | root `package.json` has `"build": "pnpm env:validate && turbo build"`                                                                                                                                                                                   |
| 753 (P2) | R       | `next/dynamic` used in dashboard pages + cluster-list (5+ files)                                                                                                                                                                                        |
| 755 (P2) | R       | `packages/db/prisma/schema.prisma` has composite indexes `@@index([authUserId, stripeCurrentPeriodEnd])` and `@@index([authUserId, plan, stripeCurrentPeriodEnd])` (lines 43–44)          |
| 728 (sec)| R*      | `docs/ci/security-scanning.md` documents the plan; actual workflow wiring is `workflows`-blocked (like #305/#522/#726/#488/#502)                                                        |
| 752 (P2) | R*      | unified logger exists in `@saasfly/common` (`logger.ts` + `createLoggerWrapper` with redaction, `logger.test.ts`); remaining console.* are JSDoc examples only                            |

**Verdict key:** R = resolved in code; R\* = substantially addressed / remaining scope is CI wiring or QA-verification, not a code defect.

### Full local verification suite (fresh, this loop)

| Check             | Command                          | Result                                                            |
| ----------------- | -------------------------------- | ----------------------------------------------------------------- |
| Install           | `pnpm install --frozen-lockfile` | ✅ Done in 7.8s (cached)                                          |
| Typecheck         | `pnpm typecheck`                 | ✅ 9/9 tasks successful                                           |
| Lint              | `pnpm lint`                      | ✅ 9/9 tasks successful, zero warnings                            |
| Tests             | `pnpm test`                      | ✅ 148 files / **2166 tests passed**                              |
| Circular deps     | `pnpm check:circular`            | ✅ no circular dependency errors                                  |
| Dependency parity | `pnpm check-deps`                | ✅ passed                                                         |
| CI (remote)       | `gh run list`                    | ✅ on-pull.yml green on `main` (success 07:29; in_progress 08:20) |

---

## 6. Genuinely Open Items (no code evidence; for maintainer)

| #       | Title                                      | Priority | Notes                                                                                     |
| ------- | ------------------------------------------ | -------- | ----------------------------------------------------------------------------------------- |
| 522     | Vercel deployment workflow                 | P3       | no Vercel workflow in `.github/workflows/` (blocked: `workflows` permission guard)        |
| 726     | check-deps in CI                           | (none)   | `check-deps` script exists but not invoked in workflow files (blocked: workflows guard)   |
| 488     | circular-dep detection in CI               | P2       | `check:circular` script exists, not wired into workflow files (blocked: workflows guard)  |
| 502     | fast-path CI workflow for routine PRs      | P2       | no dedicated fast-path workflow (blocked: workflows guard)                                |
| 305/744/670 | pnpm consistency in `iterate.yml`      | (none)   | **fix patch exists and applies cleanly** (`docs/ci/iterate-pnpm-fix.patch`); push blocked (workflows guard, fresh probe §3) |
| 668     | AI-Native cluster diagnostics              | (none)   | no implementation found — product decision required                                       |
| 749/727 | AI endpoint testing / AI code review       | (none)   | innovation proposals — product decision required                                          |
| 494     | domain layer for business logic separation | P2       | architectural refactor — maintainer decision required                                     |
| 521     | hydration consistency (remaining)          | P2       | browser-console QA verification of `useSyncExternalStore` pattern — QA task, not a defect |

---

## 7. Action Log

| Timestamp (UTC) | Action                   | Target                                           | Result                                                                                      |
| --------------- | ------------------------ | ------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| ~08:14          | Phase 0 entry scan       | PRs + issues                                     | 0 PRs, 82 issues → ISSUE MANAGER MODE                                                       |
| ~08:15          | Fresh capability probes  | issue edit/comment/close/create                  | ❌ all issue mutations 403                                                                  |
| ~08:16          | Fresh workflow probes    | new workflow file + existing workflow file       | ❌ both 403 (existing-file probe is NEW this loop)                                          |
| ~08:17          | Fresh P0/P1 spot-checks  | #496, #480, #498, #500, #501, #515, #549–551, #581 | ✅ all confirmed resolved in code                                                          |
| ~08:18          | Fresh newest-issue check | #785–#789                                        | ✅ all 5 confirmed resolved in code                                                         |
| ~08:19          | Fresh P2 re-verification | #688, #663, #664, #697, #636, #684, #753, #755, #728, #752 | ✅ resolved or R* (CI-wiring/QA only)                                        |
| ~08:20          | Repair attempt           | #305 pnpm fix via `docs/ci/iterate-pnpm-fix.patch` | ❌ patch applies cleanly but push rejected by workflows guard; branch cleaned up            |
| ~08:26          | Local verification suite | typecheck, lint, test, circular, deps            | ✅ 9/9 typecheck, 9/9 lint, 2166 tests passed, circular/deps clean                          |
| ~08:28          | Write audit report       | `docs/issue-manager-audit-2026-08-20-loop221.md` | ✅ created (PR this loop)                                                                   |

---

## 8. Decision Summary

- **Why this phase ran:** Phase 0 found 0 open PRs and 82 open issues → ISSUE MANAGER MODE per the state machine.
- **What was accomplished:** (1) fresh capability probes — issue mutations remain 403-blocked; **new: existing-workflow-file modification is also 403-blocked**, definitively closing the #305 repair path; (2) fresh P0/P1 spot-checks — all confirmed resolved, no regression; (3) fresh verdicts on the newest issues #785–#789 — all resolved in code (stripe dep, webhook secret logging, db/UI tests, ui peerDeps); (4) full local verification suite green (2166 tests); (5) confirmed repo delta vs loop220 is only the merged loop220 report (#1436).
- **Why idle (not blocked):** no repair target was attempted-and-blocked this loop beyond the workflows-guard probe; the only actionable candidates (workflow-file edits for #305/#522/#726/#488/#502) are blocked by the `workflows` permission guard with fresh documented rationale. STEP 1–3 remain token-blocked.
- **Recommended next actions for a maintainer:** unchanged from loop218–220: (1) apply `docs/ci/iterate-pnpm-fix.patch` to `iterate.yml` with a `workflows: write` token — unblocks CI + #305/#744/#670/#522/#726/#488/#502; (2) execute the loop204 §7 label normalization mapping (38 priority + 12 category + 13 dedup operations, `issues: write`); (3) close the ~75 stale-resolved issues and 8 duplicates; (4) product decisions on #494 / #749 / #668 / #727; (5) browser-console QA pass for #521.